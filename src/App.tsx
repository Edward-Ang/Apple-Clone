import React from 'react';
import RoutesConfig from './routes/Routes';

const App: React.FC = () => {
  return (
    <div className="App">
      <RoutesConfig />
    </div>
  );
};

export default App;
